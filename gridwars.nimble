version     = "0.1.0"
author      = "daveey"
description = "Grid Wars: Core Wars on a 30x30 grid for the Softmax Coworld platform. Four cogs write warrior programs, the engine fights them, a policy is just a prompt."
license     = "MIT"

srcDir = "src"

requires "nim >= 2.2.4"
requires "bitworld >= 0.1.0"
requires "mummy >= 0.4.7"
requires "curly >= 1.1.1"
requires "whisky"
